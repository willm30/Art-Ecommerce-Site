import React from "react";
import Layout from "../../components/layout/layout";

export default function Artists({ location }) {
  return (
    <Layout title="Artists" location={location}>
      <div>What should we do with this page?</div>
    </Layout>
  );
}
